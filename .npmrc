# Local Windows dev: npm cache is set by scripts/run-with-h-drive-env.cjs (H:\SOSWebsite\.local\npm-cache).
# Do not commit an absolute H: cache path here — Netlify/CI clones on Linux and must use the default cache.
